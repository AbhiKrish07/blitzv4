# ── Core backend ──────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
groq>=0.5.0
tavily-python>=0.3.0
python-dotenv>=1.0.0
pydantic>=2.0.0
httpx>=0.27.0
psutil>=5.9.0
Pillow>=10.0.0

# ── Windows-only (system control) ─────────────────────────
# These are installed automatically on Windows via system_control.py's
# lazy imports. Do NOT list them here — they have no Linux builds and
# will break Render / any Linux deployment.
#
# pywin32>=306
# pycaw>=20240210
# comtypes>=1.4.1
# pyautogui>=0.9.54
# screen-brightness-control>=0.23.0
